import { View, Text } from 'react-native';

export function Login() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Stack Login</Text>
        </View>
    );
}