import { View, Text } from 'react-native';

export function Settings() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Stack settings</Text>
        </View>
    );
}